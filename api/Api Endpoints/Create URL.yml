info:
  name: Create URL
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/shorten
  body:
    type: json
    data: |-
      {
        "url":"https://github.com/MrSplatchy"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
