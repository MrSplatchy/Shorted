info:
  name: Create URL
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/
  body:
    type: json
    data: |-
      {
        "url":"https://www.baeldung.com/cs/redirection-status-codes"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
