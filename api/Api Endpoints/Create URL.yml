info:
  name: Create URL
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost/api
  body:
    type: json
    data: |-
      {
        "url":"http://localhost:5173/"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
