info:
  name: Update Url
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/api/KHXzpN
  body:
    type: json
    data: |-
      {
        "url":"https://github.com/"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
