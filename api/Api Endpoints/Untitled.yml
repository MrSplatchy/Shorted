info:
  name: Untitled
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/shorten?url=
  params:
    - name: url
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "url":"https://gorm.io/docs/models.html#gorm-Model"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
